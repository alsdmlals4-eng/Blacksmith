name: Apply Checkpoint 005 Operating Audit Migration

on:
  push:
    branches:
      - agent/r2-checkpoint-005-postmerge-closure
    paths:
      - "tools/migrate_checkpoint_005_operating_audit_assertions.py"
      - ".github/workflows/apply-checkpoint-005-operating-audit-migration.yml"

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          ref: agent/r2-checkpoint-005-postmerge-closure
          fetch-depth: 0
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.12"
      - name: Apply assertion migration
        run: |
          python tools/migrate_checkpoint_005_operating_audit_assertions.py
          python -m py_compile tools/audit_project_operating_system.py
      - name: Commit audit assertion migration
        run: |
          set -euo pipefail
          if git diff --quiet; then
            echo "No operating audit migration required."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tools/audit_project_operating_system.py
          git commit -m "test: migrate operating audit to Checkpoint 005 closure"
          git push origin HEAD:agent/r2-checkpoint-005-postmerge-closure
