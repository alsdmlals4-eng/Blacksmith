name: Validate Blacksmith BCA Adoption

on:
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/validate-bca-visual-sheet-adoption.yml"

permissions:
  contents: read

jobs:
  contract:
    runs-on: ubuntu-latest
    steps:
      - run: echo "BCA workflow parser smoke pass"
