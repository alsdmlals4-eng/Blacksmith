name: Apply R2 Batch 006 Scoped Contract Repair

on:
  pull_request:
    types:
      - synchronize
    paths:
      - "tools/repair_r2_batch_006_scoped_implementation_contracts.py"
      - ".github/workflows/apply-r2-batch-006-scoped-contract-repair.yml"

permissions:
  contents: write

jobs:
  apply:
    if: >-
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.event.pull_request.head.ref == 'agent/close-r2-batch-006-main-canon'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout closure branch
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
      - name: Repair scoped implementation contracts
        run: python tools/repair_r2_batch_006_scoped_implementation_contracts.py
      - name: Commit repaired contracts
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No contract repair required."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add CURRENT_CONFIRMED_DECISIONS.md \
            docs/planning/BLACKSMITH_R2_BATCH_006_VERTICAL_SLICE_CANON_PROPOSAL_2026.md \
            docs/planning/R2_BATCH_006_VERTICAL_SLICE_PROPOSAL_REGISTRY.json \
            docs/planning/CURRENT_R2_CANON_REGISTRY.json \
            docs/PROJECT_OPERATING_HEALTH.json \
            '[기획서]/00_프로젝트_허브/ACTIVE_CONTEXT.md' \
            '[기획서]/00_프로젝트_허브/ROADMAP.md' \
            '[기획서]/00_프로젝트_허브/DEVELOPMENT_GATES.md' \
            tests
          git commit -m "fix: scope Batch 006 implementation approval"
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
