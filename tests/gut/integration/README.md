# GUT integration test root

This directory is an approved GUT 9.7.1 consumption root for future integration tests.
It is intentionally empty during the framework smoke adoption; zero total discovered tests is still a CI failure because the unit root contains the mandatory smoke test.
