# Blacksmith R2 고객 정보 최소 공개 정본

- 작성 시각: `2026-08-03 23:31 KST`
- Work Mode: `TOTAL_PLANNING / PLAN`
- Decision: `BS-CUSTOMER-20260803-02`
- 승인 배치: `R2_BATCH_003 / 1_OF_10 / APPROVED_PENDING_MERGE`
- 기준 main: `d8ef47a6702c3afac4eb1a10439df974a2d259df`
- 제품 구현: `BLOCKED`

## 1. 사용자 승인

판매 전 대화와 고객 카드에서 보여주는 정보는 다음 세 종류로 제한한다.

1. 사건의 대략적인 위험도
2. 고객의 핵심 능력치
3. 고객 능력과 알려진 사건 위험을 기준으로 계산한 대략적인 예상 성공률

`미확인 위험 있음`, `알 수 없는 변수가 남아 있음` 같은 별도 경고는 표시하지 않는다. 사건의 세부 전개, 내부 난이도 수치, 숨은 보정값, 난수 구조도 판매 전에 공개하지 않는다.

## 2. 공개 정보 계약

### 2.1 사건 위험도

기본 표현은 네 단계다.

```text
낮음 / 보통 / 높음 / 매우 높음
```

위험도는 사건의 알려진 규모·환경·적대 수준·요구 숙련도를 압축한 판매 판단용 정보다. 정확한 난이도 수치나 모든 위험 요인을 나열하지 않는다.

### 2.2 고객 능력치

다음 기존 정본을 유지한다.

- `기량` 1~5
- `체력` 1~5
- `판단력` 1~5
- 역할·경력
- 특기 최대 2개
- 약점 최대 1개

일정 판정은 `주 능력 1개 + 보조 능력 최대 1개 + 관련 특기·약점`을 사용한다.

### 2.3 예상 성공률

판매 전에는 고객 능력과 알려진 사건 위험을 이용해 기본 예상 성공률을 계산한다. 작품·도구·조언을 선택하면 동일 화면에서 예상 성공률을 다시 계산한다.

기본 표시 형식은 다음과 같다.

```text
사건 위험도: 높음
고객 능력: 기량 4 / 체력 3 / 판단력 4
예상 성공률: 약 60%

장비 선택 후
예상 성공률: 약 70% ↑
```

표시 원칙:

- 성공률은 가장 가까운 10% 단위로 반올림한다.
- 표시값은 최소 5%, 최대 95%로 제한한다.
- `약` 표기를 사용해 확정 결과가 아니라는 점을 자연스럽게 드러낸다.
- 판매 전 상세 보정치와 내부 계산식은 표시하지 않는다.
- 장비·도구·조언 변경 후에는 갱신된 성공률과 상승·하락 방향만 짧게 보여준다.
- 정확한 반올림 방식·상한·능력치 보정은 `BASELINE_TEST_PRESET`이다.

## 3. 판정과 UI의 분리

실제 진행 판정은 기존 구조를 유지한다.

```text
1d100
+ 고객 능력 보정
+ 작품 성능·적합성 보정
+ 도구·소모품 보정
+ 플레이어 조언·준비 보정
+ 누적 진행 상태
± 사건 변수
vs 일정 난이도
```

판매 UI는 이 전체 구조를 공개하지 않는다. 판매 판단에 필요한 사건 위험도·고객 능력치·대략적 성공률만 보여준다.

결과 화면에서는 성공·실패에 영향을 준 핵심 원인을 짧게 설명할 수 있다. 이는 판매 전 숨은 변수 경고를 추가한다는 뜻이 아니다.

## 4. 이전 결정과의 관계

`BS-CUSTOMER-20260803-01`의 고객 능력 구조와 성공 가능성 예측 원칙은 유지한다.

이번 Decision은 다음 범위만 세분화한다.

- 판매 전 공개 정보 최소화
- 5단계 성공 구간 대신 대략적인 성공률을 주 표시로 사용
- 상세 보정 이유 대신 장비 선택 전후의 성공률 변화만 표시
- 별도의 미확인 변수·숨은 위험 경고를 표시하지 않음

기존 문서에서 `미확인 위험 있음` 표시를 권장하거나, 판매 전 상세 이유 목록을 필수로 해석하는 부분은 `BS-CUSTOMER-20260803-02`가 우선한다.

## 5. 적대적 검토

### P0

- 없음.

### P1 — 대략적 확률이 확정값처럼 보일 위험

조치:

- `약` 표기
- 10% 단위 반올림
- 5~95% 표시 제한
- 실제 결과 원인에 대한 사후 설명

### P1 — 정보가 너무 적어 장비 선택 의미가 약해질 위험

조치:

- 장비 선택 전후 성공률을 같은 화면에서 즉시 갱신
- 상승·하락 방향을 표시
- 고객 능력과 작품 적합성이 모두 성공률에 영향을 주도록 유지

### P1 — 고객 능력이 작품을 압도할 위험

조치:

- 고객 능력 보정 상한 유지
- 작품·도구·조언 기여를 별도 항목으로 판정
- 고위험 사건은 능력치만으로 95%에 도달하지 않도록 테스트

### P2 — 숨은 변수 경고 제거로 불공정하게 느낄 위험

별도 경고는 표시하지 않는다. 대신 사건 위험도와 예상 성공률이 실제 결과 분포와 크게 어긋나지 않도록 플레이테스트에서 보정한다. 숨은 변수가 준비와 능력을 반복적으로 무효화하면 설계 결함으로 처리한다.

## 6. 검증할 행동 증거

- 플레이어가 위험도와 고객 능력치를 보고 성공 가능성이 높은 고객과 낮은 고객을 구분한다.
- 작품·도구를 바꾼 뒤 예상 성공률 변화 방향을 이해한다.
- 상세 계산식을 보지 않아도 판매 결정을 내릴 수 있다.
- `약 60%`를 확정 성공으로 오해하지 않는다.
- 숨은 위험 경고가 없어도 결과가 불공정했다고 반복적으로 느끼지 않는다.

## 7. 운영 상태

- 새 승인 카운터: `1/10`
- GitHub 상태: `APPROVED_PENDING_MERGE`
- 제품 코드·Scene·runtime data·asset 변경: `0`
- runtime·Android·접근성·성능·사람 플레이: `NOT_RUN`
- 제품 구현: `BLOCKED`

다음 설계 Gate는 여러 개인 일정과 세계 일정이 동시에 존재할 때 정보와 알림을 어떻게 우선순위화할지다.
